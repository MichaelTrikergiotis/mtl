# blacklist for function from stdlibc++ and libc++, we don't want to see these reports those since it is not mtl code

# functions from the stdlibc++ produce warning reports
[integer]
src:*uniform_int_dist*
src:*random.h
src:*random.tcc

# functions from the libc++ produce warning reports
[integer]
src:*algorithm