#!/usr/bin/env bash

# Runs g++ analyzer for the mtl library.

echo "This script is currently disabled because gcc analyzer doesn't support C++ yet."
echo "Check this bug in the gcc bug tracker for status updates : https://gcc.gnu.org/bugzilla/show_bug.cgi?id=97110"
